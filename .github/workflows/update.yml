name: Update Megatrend ETF Data
# Aggiorna i dati di mercato e committa data/sector_data.json
# Esecuzione automatica: ogni venerdì alle 16:00 italiane (14:00 UTC)
#   → si basa sulla chiusura di giovedì USA (dato settimanale completo)
#   → lascia ~6 ore di operatività prima della chiusura del venerdì USA (22:00 IT)
# Esecuzione manuale: dal tab Actions cliccando "Run workflow"
# Esecuzione su push: ad ogni commit su main (trigger automatico)

on:
  schedule:
    - cron: '0 14 * * 5'
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas numpy

      - name: Run update script
        run: python scripts/update_data.py

      - name: Run backtest (solo schedule/manuale)
        if: github.event_name != 'push'
        run: python scripts/backtest.py

      - name: Check generated JSON
        run: |
          if [ ! -f data/sector_data.json ]; then
            echo "ERROR: data/sector_data.json non generato"
            exit 1
          fi
          ls -la data/sector_data.json
          echo "Dimensione: $(wc -c < data/sector_data.json) byte"

      - name: Commit and push changes
        run: |
          git config --global user.name 'Sector Bot'
          git config --global user.email 'bot@users.noreply.github.com'
          git add data/
          if git diff --staged --quiet; then
            echo "Nessuna modifica ai dati"
          else
            git commit -m "Auto-update: $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push
            echo "Commit eseguito"
          fi

